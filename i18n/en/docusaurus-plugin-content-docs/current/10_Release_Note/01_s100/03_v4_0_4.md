---
sidebar_position: 3
title: "RDKS100_LNX_SDK_V4.0.4"
description: "RDKS100_LNX_SDK_V4.0.4 Release Notes"
---

# RDKS100_LNX_SDK_V4.0.4

## **Release Date:** 2025-10-24

## 1. Key Information

The **RDK S100 V4.0.4** release includes:

- **Linux Kernel:** 6.1.112-rt43
- **Bootloader:** U-Boot 2022.04
- **Root File System:** Based on Ubuntu 22.04 (Jammy), built with Multistrap
- **Supported Host Systems:**
  - Build environment: Ubuntu 20.04 (x64)
  - Flashing environment: Ubuntu 18.04 / 20.04 / 22.04 (x64)
- **Cross-compilation toolchain:** arm-gnu-toolchain-11.3.rel1 (aarch64)
- **Other components:** D-Robotics drivers, necessary firmware, toolchains, and utilities

## 2. New Features

### 2.1 Main-Domain Peripherals

#### 2.1.1 Camera

##### 2.1.1.1 GMSL Modules

- Supports the OV2312 LC031 module
- Supports the ISX031 LC032 module

##### 2.1.1.2 PYM

- Supports automatically selecting the most appropriate input size based on the configured output format

### 2.2 EtherCAT

- Supports EtherCAT IgH master build and deb installation

### 2.3 BPU

- hbm-runtime supports direct installation from the PyPI source

### 2.4 Sample

#### 2.4.1 BPU

- Supports C++ BPU examples:
  - Added C/C++ Sample for running YOLOV5X over RTSP
  - Added C/C++ Sample for yolov5x inference after video decoding
  - Added C/C++ Sample for yolov5x inference after reading images from a MIPI camera
  - Added C/C++ Sample for yolov5x inference after reading images from a USB camera
  - Added C/C++ Sample for paddleOCR model inference
  - Added C/C++ Sample for ASR model inference
  - Added C/C++ Sample for LaneNet model inference
  - Added C/C++ Sample for yoloe11 image segmentation model inference
  - Added C/C++ Sample for yolo11 pose estimation model inference
  - Added C/C++ Sample for yolo11 image segmentation model inference
  - Added C/C++ Sample for unet_mobilenet model inference
  - Added C/C++ Sample for yolo11 object detection model inference
  - Added C/C++ Sample for yolov5x object detection model inference
  - Added C/C++ Sample for Mobilenetv2 image classification model inference
  - Added C/C++ Sample for resnet18 image classification model inference

#### 2.4.2 CAN

- The CAN transparent transmission Sample supports extended frames
- Optimized the CAN transparent transmission Sample timestamp to uniformly use microseconds to meet performance statistics requirements

#### 2.4.3 SPDEV

- Display rendering framework optimization:
  - Optimized the display rendering framework efficiency
  - Optimized the resource release logic after the display rendering framework exits

### 2.5 MCU-Domain

- CAN
  - Supports extended frame transparent transmission
  - Optimized the timestamp to uniformly use microseconds to meet performance statistics requirements

## 3. Key Issue Fixes

### 3.1 Root File System

- Optimized the stability of Ubuntu applications on the S100 platform
- Fixed the issue where hobot-configs overwrites the netplan configuration

### 3.2 Main-Domain Peripherals

#### 3.2.1 Camera

- Fixed the camera synchronized frame timestamp jump caused by the kernel enabling RTC auto-update; disabled the kernel auto-synchronization of the RTC time
- Fixed the duplicate trigger timestamp issue with the OVX3C module
- Resolved the linked-list issue caused by GDC buffers not being set to null after release

#### 3.2.2 Wi-Fi/Bluetooth

- Optimized the stability of the Wi-Fi and Bluetooth drivers

### 3.3 MCU-Domain

- Fixed several stability issues

## 4. Known Issues

### 4.1 Known Software Issues

- RDK Linux
  - The RTC YSN8130E is not yet supported in software
  - Some Ubuntu applications crash under GPU acceleration due to a page size mismatch
  - The timestamp unit in the CAN transparent transmission multi-channel code does not match the timestamp unit of the Main-Domain middleware; the Main-Domain middleware prints an error, but data transmission is not affected
  - During a cold boot without an HDMI connection, the RDK S100 desktop system (GDM3) falls back to X11 rendering by default. The S100 currently does not support GPU acceleration for X11, so after the fallback CPU rendering is used, resulting in higher CPU usage. It is recommended to connect HDMI before booting the device to avoid degraded desktop rendering efficiency.
- MCU SDK
  - The MCU system currently has some stability issues
  - The BMI088 IMU on the MCU daughter board is not yet supported in software
  - Lockstep unlock is not currently supported

## 5. Upgrade Methods

### 5.1 Re-flash

Users can upgrade by re-flashing the complete image:

- Download the image: [RDK_LNX_SDK V4.0.4-Beta](https://archive.d-robotics.cc/downloads/os_images/rdk_s100/RDKS100-V4.0.4-Beta/RDK_LNX_SDK/firmwares/product.zip)
- Flash it by following the [System Flashing](../../01_Quick_start/02_install_os/rdk_s100) chapter

### 5.2 Direct Upgrade

To preserve existing root file system modifications, you can upgrade as follows:

:::warning

This upgrade requires upgrading the miniboot separately using the tool

:::

- Download the new version of the flashing tool: [D-Navigation V2.4.1](https://archive.d-robotics.cc/downloads/software_tools/download_tools/)
- Download the latest image: [RDK_LNX_SDK V4.0.4-Beta](https://archive.d-robotics.cc/downloads/os_images/rdk_s100/RDKS100-V4.0.4-Beta/RDK_LNX_SDK/firmwares/product.zip)
- Follow the uboot/usb flashing section of the [System Flashing](../../01_Quick_start/02_install_os/rdk_s100) chapter to perform basic configuration
- In the above configuration, change some options:
  - Upgrade configuration: partition selection
    - Only check: miniboot_flash, miniboot_emmc
- Restart the device after the update is complete
- Run the following commands to get the updates:

  ```bash
  sudo apt update
  sudo apt upgrade
  ```

- Restart the device again to complete the system update